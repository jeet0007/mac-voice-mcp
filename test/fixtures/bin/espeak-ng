#!/usr/bin/env node
// Stub TTS: records what would have been spoken.
const fs = require("fs");
let text = "";
process.stdin.on("data", (d) => (text += d));
process.stdin.on("end", () => fs.appendFileSync(process.env.STUB_LOG, `speak ${JSON.stringify(text)}\n`));
